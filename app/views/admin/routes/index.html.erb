
<h1> <%= t('.title') %> </h1>

<table>
  <thead>
    <tr>
      <th> <%= t('activerecord.attributes.route.name') %> </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <%= render @routes %>
  </tbody>
</table>

<br>

<%= link_to t('.add_route'), new_admin_route_path %> |
<%= link_to t('common.back'), welcome_index_path %>
